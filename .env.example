# ============================================
# 数据库配置
# ============================================

# 数据库提供商：sqlite 或 postgresql
# - sqlite: 本地开发使用，文件存储在 ./dev.db
# - postgresql: 推荐用于生产环境和 Vercel 部署
DATABASE_PROVIDER="postgresql"

# 数据库连接字符串，格式取决于 DATABASE_PROVIDER：

# PostgreSQL 配置示例（推荐）
# 本地开发：使用 Supabase 免费 PostgreSQL
# 1. 访问 https://supabase.com 注册免费账户
# 2. 创建一个 PostgreSQL 项目
# 3. 复制连接字符串
DATABASE_URL="postgresql://postgres:password@host:5432/postgres?schema=public"

# 或者本地开发：使用 Docker PostgreSQL
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pixelhub"
# 启动方法：docker-compose up -d

# Vercel 部署：使用 Vercel Postgres（自动设置）
# DATABASE_URL="postgresql://user:password@host:5432/database?schema=public"

# 其他云服务：Railway、Supabase、阿里云等
# DATABASE_URL="postgresql://user:password@host:5432/database"

# SQLite 配置示例（本地开发，不推荐生产环境）
# DATABASE_PROVIDER="sqlite"
# DATABASE_URL="file:./dev.db"

# ============================================
# 管理员账户（首次启动时创建）
# ============================================
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="admin"

# ============================================
# 会话加密密钥
# ============================================
# 生成方法：使用任意复杂字符串，生产环境建议使用 32+ 字符的随机字符串
SESSION_SECRET="complex_random_string_here"

# ============================================
# 存储配置（可选，也可在应用设置中配置）
# ============================================
# 默认存储类型：local | cos | oss | github | gitee
# STORAGE_TYPE=local

# 本地存储路径（仅当 STORAGE_TYPE=local 时有效）
# LOCAL_STORAGE_PATH=./uploads

# ============================================
# 访问控制（可选）
# ============================================
# 应用访问密码（如果设置，访问应用需要输入密码）
# ACCESS_CODE=your_password_here