# ============================================
# 数据库配置
# ============================================
# 本地开发 / Docker 部署：使用 SQLite
DATABASE_URL="file:./dev.db"

# Vercel 部署：使用 Vercel Postgres（推荐）
# 在 Vercel Dashboard 中创建 Postgres 数据库后，自动获得以下格式的连接字符串：
# DATABASE_URL="postgresql://user:password@host:5432/database?schema=public"

# 其他云服务（如 Supabase、Railway 等）：使用对应的 PostgreSQL 连接字符串
# DATABASE_URL="postgresql://user:password@host:5432/database"

# ============================================
# 管理员账户（首次启动时创建）
# ============================================
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="admin"

# ============================================
# 会话加密密钥
# ============================================
# 生成方法：使用任意复杂字符串，生产环境建议使用 32+ 字符的随机字符串
SESSION_SECRET="complex_random_string_here"

# ============================================
# 存储配置（可选，也可在应用设置中配置）
# ============================================
# 默认存储类型：local | cos | oss | github | gitee
# STORAGE_TYPE=local

# 本地存储路径（仅当 STORAGE_TYPE=local 时有效）
# LOCAL_STORAGE_PATH=./uploads

# ============================================
# 访问控制（可选）
# ============================================
# 应用访问密码（如果设置，访问应用需要输入密码）
# ACCESS_CODE=your_password_here